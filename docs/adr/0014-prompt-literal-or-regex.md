# Exec prompt is literal by default, optional regex

Exec accepts an optional `prompt` and `prompt_is_regex` (default false). Literal substring match is the safe default; regex is opt-in for multi-prompt devices. Combined with idle gap completion when no prompt is supplied.
